# Завдання 1
# Користувач вводить через кому список товарів. Виведіть цей список на екран, але без повторень назв товарів.

def display_unique_items():
    """
    Функція для виведення списку товарів без повторень.
    Користувач вводить товари через кому.
    """
    # Отримуємо введення від користувача
    input_items = input("Введіть список товарів через кому: ")

    # Розділяємо введений рядок на окремі товари
    items = [item.strip() for item in input_items.split(',')]

    # Видаляємо дублікати за допомогою множини
    unique_items = list(set(items))

    # Виводимо результат
    print("\nСписок унікальних товарів:")
    print(", ".join(unique_items))



# Завдання 2
# У магазині є два списки клієнтів: ті хто отримав знижкові
# купони, і ті хто ними скористався.
# Напишіть функцію, яка отримує 2 списки та виводить
# інформацію:
# 1. Імена тих, хто отримав купон, але не скористався,
# також вивести їх кількість
# 2. Імена шахраїв, які скористались знижкою, але магазин
# не давав їм купони

def analyze_coupons(received, used):
    """
    Аналізує списки клієнтів, які отримали купони та тих, хто ними скористався.

    Параметри:
        received - список клієнтів, які отримали купони
        used - список клієнтів, які скористались купонами
    """
    # Перетворюємо списки на множини для зручності операцій
    received_set = set(received)
    used_set = set(used)

    print("Аналіз використання купонів:")
    print("=" * 40)

    # 1. Хто отримав купон, але не скористався
    not_used = received_set - used_set
    print(f"1. Отримали купон, але не скористались ({len(not_used)}):")
    print(", ".join(not_used) if not_used else print("Немає таких клієнтів")

    # 2. Шахраї (скористались без купона)
    frauds = used_set - received_set
    print(f"\n2. Шахраї (скористались без купона) ({len(frauds)}):")
    print(", ".join(frauds) if frauds else print("Немає шахраїв"))

