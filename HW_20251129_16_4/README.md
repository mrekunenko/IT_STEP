# Модуль 16 — Використання баз даних (частина 4)
## Проста система чату з використанням мікросервісів та Redis

Проєкт складається з двох мікросервісів:

- `server.py` — сервер чату
- `client.py` — клієнт чату

Для зберігання повідомлень і статусів користувачів використовується **Redis**.